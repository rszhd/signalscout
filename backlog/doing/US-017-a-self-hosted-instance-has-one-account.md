---
id: US-017
title: A self-hosted instance has one account
type: feature
priority: p2
created: 2026-09-04T22:49+08:00
parent:
area:
resolution:
---

## Context

The first version runs on localhost and needs no login. The moment it runs on
a server with a public address, it holds API keys that spend the owner's money
and an inbox of their commercial research. An open port is then a real
problem.

Better Auth stores sessions in the same Postgres. No external service, which
matters: a self-hosted tool that requires a hosted identity provider is not
self-hosted.

Single account by default. A self-hoster is one person and does not want to
manage users. Teams are a hosted-tier question that PLAN.md defers, so the
schema should not fight a second user later, but no team UI is built here.

The first-run path is where these tools usually leak. A default password, or
an open signup form on a public address, gives the instance away to whoever
finds it first. So the first account is created by the person who starts the
instance, and signup closes after it.

## Acceptance

- [x] Better Auth with email and password, sessions in the instance's Postgres
- [x] The first run creates one account, and signup closes afterwards
- [x] There is no default password and no default account
- [x] Every route except login requires a session, asserted by a test that
      enumerates the routes rather than checking a sample
- [x] Monitors and matches are scoped to an account, so a second user is a data
      change and not a rewrite
- [ ] **Credentials are scoped to an account.** Deferred on the owner's
      decision, with the reason in the Log below. `source_credentials` stays
      keyed by provider alone.
- [x] Sessions expire, and logging out invalidates the session on the server
- [x] The self-hosting documentation says to run behind TLS and why

## Notes

- Depends on [US-001](US-001-the-workspace-runs-with-one-command.md).
- Becomes p1 before any instance is exposed to a public address, including
  our own.
- STACK.md, *The stack*, for the choice and the reason.
- No team UI. Scope the data, defer the screens.

## Log

- 2026-09-04T22:49+08:00 — Written from PLAN.md and STACK.md.
- 2026-09-08T02:26+08:00 — Started. The data was already carrying an owner:
  `monitors`, `projects`, `reply_prompts` and `feedback` have had a `NOT NULL`
  `user_id` since they were written, filled with the constant `singleUserId =
  "self-hosted"`. So most of this ticket is replacing one word with a session,
  not reshaping four tables.
- 2026-09-08T02:30+08:00 — **The credentials box is deferred, and this is the
  reasoning.** The owner's plan is `.env` for self-hosted and per-account keys
  for a cloud tier. Two things settle where that leaves this ticket. The
  `source_credentials` lookup falls back to the process environment —
  `BRIGHTDATA_API_KEY` and its siblings — and an environment variable cannot
  belong to a person, so scoping only the database half would make the two
  halves of one lookup disagree: a second account's monitor would still poll on
  the instance's `.env` key, and the scoping would look real without being
  real. And adding the column later is cheap, which corrected an argument made
  earlier in this same session: `source_credentials.record` is **stored per
  row**, so a row keeps decrypting under the name it was written with, and
  changing the naming rule re-encrypts nothing. US-024 already proved that
  path. So the cloud ticket pays a migration and a threaded owner, not a
  re-encryption, and it is also the ticket that decides the `.env`-versus-
  account rule this one cannot answer.
- 2026-09-08T02:38+08:00 — Better Auth 1.7.3, four tables in migration 0043,
  declared in `schema.ts` rather than generated by Better Auth's own CLI: this
  repository has one migration path, and a second tool writing tables into the
  same database would take the "one migration number, one file" rule away. The
  library addresses a Drizzle schema by the key a table is exported under and
  each column by its **property name**, so every property is the camel-case
  field name it asks for while the SQL column beside it stays snake case.
  Rename a property and it stops finding the column, with no type error.
- 2026-09-08T02:40+08:00 — **The gate is one hook, and the test enumerates.**
  `apps/api/src/auth.ts` installs an `onRequest` hook on the root instance
  before any route is registered, and an `onRoute` hook that records what was
  registered. `auth.test.ts` walks that list: **53 method-and-path pairs, every
  one 401 without a cookie**, against a written open list of three —
  `/api/auth/*`, `/api/health`, `/api/auth-status`. A sample of five would pass
  for ever and say nothing about the sixth, which is the whole failure shape
  here.
- 2026-09-08T02:41+08:00 — Signup closes in `databaseHooks.user.create.before`
  rather than in `disableSignUp`, because the answer is not a setting: it is
  whether `users` is empty, and it changes once with no restart. Every path
  that creates a user — the sign-up endpoint, a social provider somebody
  enables later, an admin plugin — goes through that hook and only that hook.
- 2026-09-08T02:42+08:00 — The first account adopts the rows that predate it,
  in the same transaction that creates it. Without it, upgrading an instance
  that already polls shows empty screens over intact data, which reads as data
  loss and is the worst way to meet a new login screen. `monitors.user_id`
  still carries no foreign key, deliberately: a monitor may be older than the
  first account, so the constraint would refuse to apply on exactly the
  instances whose data is worth keeping.
- 2026-09-08T02:44+08:00 — **A rule was moved out of core after two tests
  disagreed with it.** The first version refused a verdict from anybody but the
  monitor's owner, inside `recordVerdict`. Two existing core tests went red:
  `feedback` is one row per match *per person* by design, and the schema keeps
  that so teams are a data change later. Removing a capability that has tests,
  inside a ticket that says "defer the screens", is not this ticket's call. The
  rule now lives at the route — you may judge what you can read — and core's
  mechanism is untouched. `matchOwner` is the one query it needs.
- 2026-09-08T02:47+08:00 — **Driven live against the built app**, on a scratch
  database seeded the old way, `NODE_ENV=production`. `GET /` 200 signed out;
  `GET /api/monitors` 401; sign-up 200; a second sign-up **403** with the
  instance's own sentence; the monitor and project written under `self-hosted`
  came back under the new account's id. The Chrome extension was not connected,
  so **no real browser has rendered the login screen** — jsdom for the screen,
  `curl` for the server half.
- 2026-09-08T02:48+08:00 — **That run found something the suite could not
  see.** Sign-out with a valid cookie and no `Origin` answered **403
  `MISSING_OR_NULL_ORIGIN`** and the session survived; with the right origin it
  answered 200, the `sessions` row was gone, and the old cookie then got 401. A
  test asserting the strict branch was written, could not reach it, and was
  deleted rather than weakened, with the measurement recorded in a comment. See
  the entry four lines down: that was the wrong repair, and the reason it was
  wrong is the same reason `pnpm dev` broke.
- 2026-09-08T03:02+08:00 — **The login refused every attempt under `pnpm dev`,
  reported by the owner.** Reproduced in one request: Vite serves the UI on
  5173 and proxies `/api` to 3000 with `changeOrigin`, so Fastify receives the
  host rewritten to 3000 beside the browser's untouched `Origin:
  http://localhost:5173`. Better Auth compares them and answers **403
  `INVALID_ORIGIN`**. Production, where one Fastify serves both, was never
  affected — which is exactly why the live run earlier that evening passed.
- 2026-09-08T03:05+08:00 — Fixed with a list rather than a switch.
  `createAuth` takes `trustedOrigins`; `server.ts` builds it from
  `AUTH_TRUSTED_ORIGINS` and adds the Vite dev server while `NODE_ENV` is
  development, never in production. "Accept any origin" was not considered:
  that is precisely what a cross-site request forgery needs, and this check is
  what refuses it. Measured both ways against the built app — development
  accepts `http://localhost:5173` and refuses `http://evil.example`, production
  refuses `http://localhost:5173`.
- 2026-09-08T03:06+08:00 — **The earlier deletion was the wrong repair, and
  this is what it hid.** The reason no test could reach the branch is
  `create-context.mjs`: `skipOriginCheck: … isTest() ? true : false`. Better
  Auth turns the whole check off when `NODE_ENV` is `test`. That is worse than
  an untestable branch — a deployment whose `NODE_ENV` is not what its owner
  thinks loses cross-site protection silently, with every screen still working.
  `advanced.disableOriginCheck: false` states it once for every environment.
  The check is now the same everywhere, and the deleted test is back and
  passing: no origin is 403, the wrong origin is 403, the session survives
  both, and only the right origin ends it. **A branch a test cannot reach is
  worth asking why about, not worth writing a comment about.**
- 2026-09-08T03:10+08:00 — **In `doing/`, not `done/`, because one acceptance
  box is open.** The login itself is finished and running; the credentials box
  is deferred with the reasoning above, and deferring a box is the owner's call
  rather than mine. Tick it, or split it into the cloud-tier ticket, and this
  moves. 1,390 tests pass, `pnpm lint` and
  `pnpm typecheck` clean, migration 0043 applied to the development database
  and its 21 existing monitors. `docs/accounts.md` is new and holds the TLS
  reasoning, the `X-Forwarded-Proto` header a proxy must send, and the SQL for
  getting back in — there is no password reset, because nothing here sends
  mail and a reset link that never arrives is worse than no button.
